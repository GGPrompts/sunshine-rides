import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import { Header } from "@/components/header";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sunshine Rides | Your Ride, Your Way",
  description: "Reliable transportation services in Colorado. Wheelchair accessible, medical transport, and more. Our car is your car.",
  keywords: ["transportation", "rides", "Colorado", "wheelchair accessible", "medical transport", "Medicaid"],
  authors: [{ name: "Sunshine Rides of Colorado" }],
  openGraph: {
    title: "Sunshine Rides | Your Ride, Your Way",
    description: "Reliable transportation services in Colorado. Our car is your car.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${inter.variable} ${geistMono.variable} font-sans antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          <Header />
          <main>{children}</main>
        </ThemeProvider>
      </body>
    </html>
  );
}
